import PageHero from '../components/PageHero';import {impact} from '../content';
export default function Impact(){return <><PageHero eyebrow="Documented Impact" title="Milestones recorded in Foundation reports." text="The figures below are historical snapshots drawn from annual-report material and should not be interpreted as current-year totals."/><section className="section impact impact-page"><div className="container"><div className="impact-grid">{impact.map(([n,t])=><article className="impact-card" key={n+t}><strong>{n}</strong><span>{t}</span></article>)}</div><p className="impact-disclaimer">For current impact reporting, the latest verified program data can be added before production launch.</p></div></section></>}
